{
  "name": "loomide",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "src-ui"
  ],
  "scripts": {
    "dev": "tauri dev --config src-core/tauri.conf.json",
    "tauri:build": "tauri build --config src-core/tauri.conf.json",
    "build": "npm run build --workspace src-ui",
    "typecheck": "npm run typecheck --workspace src-ui",
    "lint": "npm run typecheck"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
